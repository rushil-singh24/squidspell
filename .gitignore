# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# Node
node_modules/
frontend/dist/

# Env secrets
.env
.env.*
!.env.example

# ML artifacts. Trained models + metrics ARE committed now (Phase 10) so the
# deployed backend/frontend can build from a clean checkout. Raw capture data
# and regenerable diagnostic plots stay out.
ml/data/*.csv
ml/data/motion_sequences/
ml/results/*.png

# Caches
.pytest_cache/
frontend/.vite/

# OS
.DS_Store
